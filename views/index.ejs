<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Home - E-commerce</title>
  <link rel="stylesheet" href="/css/style.css">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
  <style>
    body { background: linear-gradient(120deg, #f6f8fa 0%, #eaf6fd 100%); }
    .main-header { position: sticky; top: 0; left: 0; width: 100vw; z-index: 200; background: linear-gradient(90deg, #3498db 0%, #6dd5ed 100%); box-shadow: 0 2px 16px rgba(44,62,80,0.10); display: flex; align-items: center; justify-content: space-between; padding: 0.7rem 2.5vw 0.7rem 2vw; min-height: 70px; }
    .main-header .logo { font-size: 2rem; font-weight: 700; color: #fff; letter-spacing: 1px; display: flex; align-items: center; gap: 0.7rem; }
    .main-header .logo i { font-size: 2.2rem; color: #fff; }
    .main-header nav ul { display: flex; gap: 1.5rem; list-style: none; margin: 0; padding: 0; }
    .main-header nav a { color: #fff; font-weight: 600; font-size: 1.08rem; text-decoration: none; padding: 0.5rem 1.2rem; border-radius: 8px; transition: background 0.2s, color 0.2s; }
    .main-header nav a:hover, .main-header nav a.active { background: #fff; color: #3498db; }
    .hero-section { display: flex; flex-direction: column; align-items: center; justify-content: center; min-height: 60vh; padding: 5vh 2vw 2vh 2vw; position: relative; text-align: center; overflow: hidden; }
    .hero-video-bg { position: absolute; top: 0; left: 0; width: 100%; height: 100%; object-fit: cover; z-index: 0; opacity: 0.45; pointer-events: none; }
    .hero-bg { position: absolute; top: 0; left: 0; width: 100%; height: 100%; z-index: 0; background: linear-gradient(120deg, #3498db 0%, #6dd5ed 100%); opacity: 0.13; pointer-events: none; }
    .hero-content { position: relative; z-index: 2; max-width: 700px; margin: 0 auto; }
    .hero-title { font-size: 2.8rem; font-weight: 800; color: #222; margin-bottom: 1.2rem; letter-spacing: 1px; text-shadow: 0 4px 32px rgba(0,0,0,0.08); }
    .hero-desc { color: #444; font-size: 1.25rem; margin-bottom: 2.5rem; text-shadow: 0 2px 8px rgba(0,0,0,0.08); }
    .hero-btn { background: linear-gradient(90deg, #3498db 0%, #6dd5ed 100%); color: #fff; padding: 1.1rem 2.7rem; border: none; border-radius: 12px; font-size: 1.25rem; font-weight: 700; box-shadow: 0 2px 16px rgba(52,152,219,0.18); cursor: pointer; margin-top: 1.5rem; transition: background 0.3s, transform 0.2s; }
    .hero-btn:hover { background: linear-gradient(90deg, #6dd5ed 0%, #3498db 100%); transform: translateY(-2px) scale(1.04); }
    .features-section { display: flex; flex-wrap: wrap; gap: 2rem; justify-content: center; margin: 4rem 0 2rem 0; }
    .feature-card { background: #fff; border-radius: 16px; box-shadow: 0 2px 16px rgba(44,62,80,0.10); padding: 2rem 1.5rem; min-width: 220px; max-width: 320px; flex: 1; display: flex; flex-direction: column; align-items: center; text-align: center; transition: box-shadow 0.2s, transform 0.2s; }
    .feature-card:hover { box-shadow: 0 8px 32px rgba(44,62,80,0.18); transform: translateY(-4px) scale(1.03); }
    .feature-card i { font-size: 2.5rem; color: #3498db; margin-bottom: 1rem; }
    .feature-title { font-size: 1.18rem; font-weight: 700; color: #222; margin-bottom: 0.5rem; }
    .feature-desc { color: #666; font-size: 1.05rem; }
    .frosted-glass { background: rgba(255,255,255,0.15); box-shadow: 0 8px 32px 0 rgba(31,38,135,0.17); backdrop-filter: blur(8px); -webkit-backdrop-filter: blur(8px); border-radius: 20px; border: 1px solid rgba(255,255,255,0.18); padding: 3rem 2.5rem; max-width: 700px; margin: 0 auto 2.5rem auto; text-align: center; }
    #about h2, #contact h2 { color: #3498db; margin-bottom: 1rem; }
    #about p, #contact p { color: #444; font-size: 1.1rem; margin-bottom: 1.2rem; }
    #contact form label { display: block; margin-bottom: 0.5rem; font-weight: 500; color: #2c3e50; text-align: left; }
    #contact form input[type="text"], #contact form input[type="email"], #contact form textarea { width: 100%; padding: 0.75rem; margin-bottom: 1rem; border: 1px solid #ddd; border-radius: 4px; font-size: 1rem; background: rgba(255,255,255,0.7); }
    #contact form textarea { resize: vertical; min-height: 100px; font-family: inherit; }
    #contact form button[type="submit"] { background: linear-gradient(90deg, #3498db 0%, #6dd5ed 100%); color: #fff; padding: 0.7rem 0; border-radius: 8px; font-size: 1.08rem; font-weight: 600; box-shadow: 0 2px 8px rgba(52,152,219,0.10); border: none; cursor: pointer; transition: background 0.2s, transform 0.2s; }
    #contact form button[type="submit"]:hover { background: linear-gradient(90deg, #6dd5ed 0%, #3498db 100%); transform: scale(1.03); }
    @media (max-width: 900px) { .features-section { flex-direction: column; gap: 1.2rem; } .main-header { flex-direction: column; align-items: stretch; padding: 0.7rem 1vw; } }
    @media (max-width: 700px) { .hero-title { font-size: 2rem; } .main-header .logo { font-size: 1.3rem; } .frosted-glass { padding: 1.5rem 0.5rem; max-width: 98vw; } .features-section { gap: 0.7rem; } }
  </style>
</head>
<body>
  <!-- Sticky Header -->
  <header class="main-header">
    <div class="logo"><i class="fa fa-store"></i> ZupZup</div>
    <nav>
      <ul>
        <li><a href="/products">Products</a></li>
        <li><a href="#about">About</a></li>
        <li><a href="#contact">Contact Us</a></li>
        <% if (typeof session !== 'undefined' && session.username) { %>
          <li><a href="/cart">Cart</a></li>
          <li><a href="/orders">Orders</a></li>
          <li><a href="/logout">Logout</a></li>
        <% } else { %>
          <li><a href="/login">Login</a></li>
          <li><a href="/register">Register</a></li>
        <% } %>
      </ul>
    </nav>
  </header>
  <!-- Hero Section -->
  <section class="hero-section" style="position:relative;overflow:hidden;">
    <video id="heroVideo" autoplay loop muted playsinline class="hero-video-bg" style="position:absolute;top:0;left:0;width:100%;height:100%;object-fit:cover;z-index:0;opacity:0.45;">
      <source src="/css/vedios/ZupZUP.mp4" type="video/mp4">
      Your browser does not support the video tag.
    </video>
    <button id="unmuteHeroVideo" style="position:absolute;bottom:32px;right:32px;z-index:12;background:rgba(255,255,255,0.92);border:none;border-radius:50%;width:64px;height:64px;display:flex;align-items:center;justify-content:center;box-shadow:0 2px 16px rgba(44,62,80,0.18);font-size:2rem;color:#3498db;cursor:pointer;transition:background 0.2s,transform 0.2s;">
      <i class="fa fa-volume-up"></i>
    </button>
    <div class="hero-bg"></div>
    <div class="hero-content" style="position:relative;z-index:2;">
      <h1 class="hero-title">Welcome to ZupZup – Your Ultimate Shopping Destination</h1>
      <p class="hero-desc">Discover the best deals, exclusive offers, and a seamless shopping experience. Shop from a curated collection of products with fast delivery and secure payments.</p>
      <a href="/products" class="hero-btn"><i class="fa fa-shopping-bag"></i> Shop Now</a>
    </div>
    <div style="position:absolute;top:0;left:0;width:100%;height:100%;background:linear-gradient(120deg,rgba(52,152,219,0.18) 0%,rgba(109,213,237,0.18) 100%);z-index:1;"></div>
    <script>
      const heroVideo = document.getElementById('heroVideo');
      const unmuteBtn = document.getElementById('unmuteHeroVideo');
      unmuteBtn.addEventListener('click', function() {
        heroVideo.muted = false;
        heroVideo.volume = 1.0;
        unmuteBtn.style.display = 'none';
      });
    </script>
  </section>
  <!-- Features Section -->
  <section class="features-section">
    <div class="feature-card">
      <i class="fa fa-truck"></i>
      <div class="feature-title">Fast & Free Delivery</div>
      <div class="feature-desc">Get your orders delivered quickly and for free on eligible products.</div>
    </div>
    <div class="feature-card">
      <i class="fa fa-shield-alt"></i>
      <div class="feature-title">Secure Payments</div>
      <div class="feature-desc">Your transactions are protected with industry-leading security.</div>
    </div>
    <div class="feature-card">
      <i class="fa fa-tags"></i>
      <div class="feature-title">Best Deals & Offers</div>
      <div class="feature-desc">Enjoy exclusive discounts and special offers every day.</div>
    </div>
    <div class="feature-card">
      <i class="fa fa-headset"></i>
      <div class="feature-title">24/7 Customer Support</div>
      <div class="feature-desc">We’re here to help you anytime, anywhere.</div>
    </div>
  </section>
  <!-- About Section -->
  <section id="about" class="frosted-glass">
    <h2>About ZupZup</h2>
    <p>ZupZup is a modern e-commerce platform designed to provide a seamless, secure, and enjoyable shopping experience. We offer a curated selection of products across categories, fast delivery, and reliable customer support. Our mission is to make online shopping easy and delightful for everyone.</p>
  </section>
  <!-- Contact Us Section -->
  <section id="contact" class="frosted-glass">
    <h2>Contact Us</h2>
    <form action="/contact" method="POST" style="max-width:500px;margin:0 auto;">
      <label for="contact-name">Name:</label>
      <input type="text" id="contact-name" name="name" required>
      <label for="contact-email">Email:</label>
      <input type="email" id="contact-email" name="email" required>
      <label for="contact-message">Message:</label>
      <textarea id="contact-message" name="message" rows="4" required></textarea>
      <button type="submit">Send Message</button>
    </form>
    <div style="margin-top:1.5rem;text-align:center;color:#555;">
      <p><strong>Email:</strong> support@zupzup.com</p>
      <p><strong>Phone:</strong> +91-12345-67890</p>
    </div>
  </section>
</body>
</html>