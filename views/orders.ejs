<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>My Orders - ZupZup</title>
  <link rel="stylesheet" href="/css/style.css">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
  <style>
    /* Enhanced Orders Page Design */
    body {
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      min-height: 100vh;
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    }

    /* Header Section */
    .orders-header-section {
      background: rgba(255, 255, 255, 0.95);
      backdrop-filter: blur(20px);
      border-radius: 25px;
      padding: 2.5rem;
      margin-bottom: 2rem;
      text-align: center;
      box-shadow: 0 15px 40px rgba(0, 0, 0, 0.1);
      border: 1px solid rgba(255, 255, 255, 0.2);
    }

    .orders-header-icon {
      font-size: 3.5rem;
      color: #3498db;
      margin-bottom: 1rem;
      animation: bounce 2s ease-in-out infinite;
    }

    .orders-title {
      font-size: 2.5rem;
      font-weight: 800;
      background: linear-gradient(45deg, #2c3e50, #3498db, #2c3e50);
      background-size: 200% 200%;
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
      animation: shimmer 3s ease-in-out infinite;
      margin-bottom: 0.5rem;
    }

    .orders-subtitle {
      color: #666;
      font-size: 1.1rem;
      margin-bottom: 0;
    }

    /* Main Container */
    .orders-main {
      max-width: 1200px;
      margin: 2rem auto;
      padding: 0 1rem;
    }

    /* Order Cards */
    .orders-list {
      display: flex;
      flex-direction: column;
      gap: 2rem;
    }

    .order-card {
      background: rgba(255, 255, 255, 0.95);
      backdrop-filter: blur(20px);
      border-radius: 25px;
      box-shadow: 0 15px 40px rgba(0, 0, 0, 0.1);
      border: 1px solid rgba(255, 255, 255, 0.2);
      overflow: hidden;
      transition: all 0.3s ease;
      animation: fadeInUp 0.8s ease-out;
    }

    /* Animation delays for order cards */
    .order-card-0 { animation-delay: 0s; }
    .order-card-1 { animation-delay: 0.1s; }
    .order-card-2 { animation-delay: 0.2s; }
    .order-card-3 { animation-delay: 0.3s; }
    .order-card-4 { animation-delay: 0.4s; }
    .order-card-5 { animation-delay: 0.5s; }

    .order-card:hover {
      transform: translateY(-5px);
      box-shadow: 0 20px 50px rgba(0, 0, 0, 0.15);
    }

    /* Order Header */
    .order-header {
      background: linear-gradient(135deg, #3498db, #6dd5ed);
      padding: 1.5rem 2rem;
      display: flex;
      justify-content: space-between;
      align-items: center;
      flex-wrap: wrap;
      gap: 1rem;
    }

    .order-info {
      display: flex;
      flex-direction: column;
      gap: 0.5rem;
    }

    .order-id {
      color: rgba(255, 255, 255, 0.9);
      font-size: 0.95rem;
      font-weight: 500;
    }

    .order-date {
      color: rgba(255, 255, 255, 0.8);
      font-size: 0.9rem;
    }

    .order-status {
      padding: 0.6rem 1.5rem;
      border-radius: 25px;
      font-weight: 600;
      font-size: 0.95rem;
      display: flex;
      align-items: center;
      gap: 0.5rem;
      box-shadow: 0 4px 15px rgba(0, 0, 0, 0.1);
    }

    .status-completed {
      background: linear-gradient(45deg, #27ae60, #2ecc71);
      color: white;
    }

    .status-pending {
      background: linear-gradient(45deg, #f39c12, #f1c40f);
      color: white;
    }

    .status-failed {
      background: linear-gradient(45deg, #e74c3c, #c0392b);
      color: white;
    }

    /* Order Items */
    .order-items {
      padding: 2rem;
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
      gap: 1.5rem;
    }

    .order-item {
      background: #f8f9fa;
      border-radius: 20px;
      padding: 1.5rem;
      display: flex;
      align-items: center;
      gap: 1.2rem;
      transition: all 0.3s ease;
      border: 1px solid rgba(0, 0, 0, 0.05);
    }

    .order-item:hover {
      transform: translateY(-3px);
      box-shadow: 0 10px 25px rgba(0, 0, 0, 0.1);
      background: #fff;
    }

    .item-image {
      width: 80px;
      height: 80px;
      border-radius: 15px;
      object-fit: cover;
      box-shadow: 0 4px 15px rgba(0, 0, 0, 0.1);
      transition: transform 0.3s ease;
    }

    .order-item:hover .item-image {
      transform: scale(1.1);
    }

    .item-info {
      flex: 1;
    }

    .item-name {
      font-size: 1.1rem;
      font-weight: 600;
      color: #2c3e50;
      margin-bottom: 0.5rem;
      line-height: 1.3;
    }

    .item-details {
      display: flex;
      flex-direction: column;
      gap: 0.3rem;
    }

    .item-detail {
      font-size: 0.9rem;
      color: #666;
      display: flex;
      align-items: center;
      gap: 0.5rem;
    }

    .item-price {
      font-weight: 600;
      color: #27ae60;
    }

    .item-quantity {
      background: #e8f5e8;
      color: #27ae60;
      padding: 0.2rem 0.6rem;
      border-radius: 12px;
      font-size: 0.8rem;
      font-weight: 600;
    }

    /* Order Summary */
    .order-summary {
      background: linear-gradient(135deg, #f8f9fa, #e9ecef);
      padding: 1.5rem 2rem;
      display: flex;
      justify-content: space-between;
      align-items: center;
      border-top: 1px solid rgba(0, 0, 0, 0.05);
    }

    .order-total {
      font-size: 1.3rem;
      font-weight: 700;
      color: #2c3e50;
    }

    .total-amount {
      font-size: 1.5rem;
      font-weight: 800;
      background: linear-gradient(45deg, #27ae60, #2ecc71);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
    }

    /* Empty State */
    .empty-orders {
      text-align: center;
      padding: 4rem 2rem;
      background: rgba(255, 255, 255, 0.95);
      backdrop-filter: blur(20px);
      border-radius: 25px;
      box-shadow: 0 15px 40px rgba(0, 0, 0, 0.1);
    }

    .empty-icon {
      font-size: 4rem;
      color: #bdc3c7;
      margin-bottom: 1.5rem;
      animation: bounce 2s ease-in-out infinite;
    }

    .empty-text {
      font-size: 1.3rem;
      color: #7f8c8d;
      margin-bottom: 2rem;
    }

    /* Action Buttons */
    .action-buttons {
      display: flex;
      justify-content: center;
      gap: 1rem;
      margin-top: 2rem;
      flex-wrap: wrap;
    }

    .action-btn {
      background: linear-gradient(45deg, #3498db, #6dd5ed);
      color: white;
      padding: 1rem 2rem;
      border-radius: 25px;
      text-decoration: none;
      font-weight: 600;
      font-size: 1rem;
      transition: all 0.3s ease;
      box-shadow: 0 4px 15px rgba(52, 152, 219, 0.3);
      display: flex;
      align-items: center;
      gap: 0.5rem;
    }

    .action-btn:hover {
      transform: translateY(-3px);
      box-shadow: 0 8px 25px rgba(52, 152, 219, 0.4);
      color: white;
    }

    .action-btn.secondary {
      background: linear-gradient(45deg, #27ae60, #2ecc71);
      box-shadow: 0 4px 15px rgba(39, 174, 96, 0.3);
    }

    .action-btn.secondary:hover {
      box-shadow: 0 8px 25px rgba(39, 174, 96, 0.4);
    }

    /* Animations */
    @keyframes fadeInUp {
      from {
        opacity: 0;
        transform: translateY(30px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    @keyframes bounce {
      0%, 20%, 50%, 80%, 100% {
        transform: translateY(0);
      }
      40% {
        transform: translateY(-10px);
      }
      60% {
        transform: translateY(-5px);
      }
    }

    @keyframes shimmer {
      0%, 100% {
        background-position: 0% 50%;
      }
      50% {
        background-position: 100% 50%;
      }
    }

    /* Responsive Design */
    @media (max-width: 768px) {
      .orders-header-section {
        padding: 2rem 1rem;
      }

      .orders-title {
        font-size: 2rem;
      }

      .order-header {
        flex-direction: column;
        align-items: flex-start;
        gap: 1rem;
      }

      .order-items {
        grid-template-columns: 1fr;
        padding: 1.5rem;
      }

      .order-item {
        flex-direction: column;
        text-align: center;
        gap: 1rem;
      }

      .order-summary {
        flex-direction: column;
        gap: 1rem;
        text-align: center;
      }

      .action-buttons {
        flex-direction: column;
        align-items: center;
      }
    }
  </style>
</head>
<body>
  <div class="orders-main">
    <!-- Enhanced Header Section -->
    <div class="orders-header-section">
      <div class="orders-header-icon">
        <i class="fa fa-shopping-bag"></i>
      </div>
      <h1 class="orders-title">My Orders</h1>
      <p class="orders-subtitle">Track your purchase history and order status</p>
    </div>

    <% if (orders && orders.length > 0) { %>
      <div class="orders-list">
        <% orders.forEach((order, index) => { %>
          <div class="order-card order-card-<%= index %>">
            <!-- Order Header -->
            <div class="order-header">
              <div class="order-info">
                <div class="order-id">
                  <i class="fa fa-hashtag"></i> Order ID: <%= order._id %>
                </div>
                <div class="order-date">
                  <i class="fa fa-calendar"></i> 
                  <%= new Date(order.createdAt).toLocaleDateString('en-US', { 
                    year: 'numeric', 
                    month: 'long', 
                    day: 'numeric',
                    hour: '2-digit',
                    minute: '2-digit'
                  }) %>
                </div>
              </div>
              
              <% if (order.status === 'completed') { %>
                <span class="order-status status-completed">
                  <i class="fa fa-check-circle"></i> Completed
                </span>
              <% } else if (order.status === 'pending') { %>
                <span class="order-status status-pending">
                  <i class="fa fa-clock"></i> Pending
                </span>
              <% } else { %>
                <span class="order-status status-failed">
                  <i class="fa fa-times-circle"></i> Failed
                </span>
              <% } %>
            </div>

            <!-- Order Items -->
            <div class="order-items">
              <% order.items.forEach(item => { %>
                <div class="order-item">
                  <% if (item.productId && item.productId.imageUrl) { %>
                    <img src="<%= item.productId.imageUrl %>" alt="<%= item.name %>" class="item-image">
                  <% } else { %>
                    <div class="item-image" style="background: #e9ecef; display: flex; align-items: center; justify-content: center;">
                      <i class="fa fa-image" style="font-size: 2rem; color: #bdc3c7;"></i>
                    </div>
                  <% } %>
                  
                  <div class="item-info">
                    <h4 class="item-name"><%= item.name %></h4>
                    <div class="item-details">
                      <div class="item-detail">
                        <i class="fa fa-tag"></i>
                        <span class="item-price">₹<%= item.price.toLocaleString() %></span>
                      </div>
                      <div class="item-detail">
                        <i class="fa fa-shopping-cart"></i>
                        <span class="item-quantity">Qty: <%= item.quantity %></span>
                      </div>
                      <div class="item-detail">
                        <i class="fa fa-calculator"></i>
                        <span>Subtotal: ₹<%= (item.price * item.quantity).toLocaleString() %></span>
                      </div>
                    </div>
                  </div>
                </div>
              <% }); %>
            </div>

            <!-- Order Summary -->
            <div class="order-summary">
              <div class="order-total">
                <i class="fa fa-receipt"></i> Order Total:
              </div>
              <div class="total-amount">
                ₹<%= order.totalAmount.toLocaleString() %>
              </div>
            </div>
          </div>
        <% }); %>
      </div>
    <% } else { %>
      <div class="empty-orders">
        <div class="empty-icon">
          <i class="fa fa-box-open"></i>
        </div>
        <h2 class="empty-text">No Orders Yet</h2>
        <p style="color: #95a5a6; margin-bottom: 2rem;">Start shopping to see your order history here!</p>
      </div>
    <% } %>

    <!-- Action Buttons -->
    <div class="action-buttons">
      <a href="/products" class="action-btn">
        <i class="fa fa-shopping-bag"></i> Continue Shopping
      </a>
      <a href="/" class="action-btn secondary">
        <i class="fa fa-home"></i> Back to Home
      </a>
    </div>
  </div>
</body>
</html> 